// === ARRAY ===

// Definition: Ordered list of items stored sequentially in memory.

// Big O:
    // Lookup: O(1)
    // Push: O(1)
    // Insert: O(n)
    // Delete: O(n)
    // Search: O(n)

    // *Push/append can sometimes be O(n)

// IMPLEMENTATION:
    